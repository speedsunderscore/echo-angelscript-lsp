import { HASH_SET_AS } from './hash_set';
import { HASH_MAP_AS } from './hash_map';

/** Echo "Containers" category -- hash set, hash map. */
export const CONTAINERS_SOURCES: ReadonlyArray<readonly [string, string]> = [
  ['containers/hash_set.as', HASH_SET_AS],
  ['containers/hash_map.as', HASH_MAP_AS],
];
